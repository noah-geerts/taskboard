services:
  db:
    image: postgres:16 # Latest stable Postgres version
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_USER: testuser # DB username
      POSTGRES_PASSWORD: testpass # DB password
      POSTGRES_DB: testdb # Initial database
    ports:
      - "5432:5432" # Expose Postgres on host
